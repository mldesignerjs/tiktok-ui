// import { createSelector } from '@reduxjs/toolkit'

export const volumeVideoSelector = (state) => state.volumeVideo
export const currentVideoPlayingSelector = (state) => state.currentVideoPlaying
export const currentUserSelector = (state) => state.user.currentUser
